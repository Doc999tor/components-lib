.modal-empty-name
  display flex
  align-items center
  padding 4rem 0 4rem
  background-color var(--main-bgcolor)
  label
    margin 0 !important
  .modal-empty-name-footer
    display flex
    button
      background-color transparent
      text-transform uppercase
      border none
      width 50%
      display flex
      align-items center
      justify-content center
      height 3.75rem
    .no-btn
      outline none
      color var(--dark-color)
      font-size 14px
      padding 0